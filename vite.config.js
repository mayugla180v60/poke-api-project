import { defineConfig } from "vite"

export default defineConfig({
  build: {
    base: "/api-test",
    // root: "/api-test",
    assetsDir: "./assets",
    // rollupOptions: {
      
    // },
  },
})