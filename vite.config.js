import { defineConfig } from "vite"

export default defineConfig({
  base: "/api-test",
  build: {
    
    // root: "/api-test",
    assetsDir: "./assets",
    // rollupOptions: {
      
    // },
  },
})